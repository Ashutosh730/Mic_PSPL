<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mic">
        <activity android:name=".VideoDetail"></activity>
        <activity android:name=".CreatePassword" />
        <activity android:name=".ForgotPassword" />
        <activity
            android:name=".AccountCreated"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".Final"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".Payment" />
        <activity android:name=".ConfirmationDetails" />
        <activity android:name=".UserDashBoad" />
        <activity android:name=".HomePage" />
        <activity
            android:name=".SignUp"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>